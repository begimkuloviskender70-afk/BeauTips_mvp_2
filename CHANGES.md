# 🔄 Изменения для деплоя

## Что было изменено

### Frontend - Пути к ресурсам

✅ **Все абсолютные пути заменены на относительные**

#### Было:
```html
<link rel="stylesheet" href="/static/base.css">
<img src="/static/images/logo.png">
<script src="/static/api.js"></script>
<a href="/static/index.html">
```

#### Стало:
```html
<link rel="stylesheet" href="./base.css">
<img src="./images/logo.png">
<script src="./api.js"></script>
<a href="./index.html">
```

### Затронутые файлы:
- ✅ `index.html` - главная страница
- ✅ `chat.html` - интерфейс квиза
- ✅ `history.html` - история тестов
- ✅ `profile.html` - профиль пользователя
- ✅ `results.html` - результаты теста
- ✅ `register.html` - регистрация/вход
- ✅ `verify-email.html` - верификация email
- ✅ `api.js` - API клиент
- ✅ `auth.js` - аутентификация
- ✅ `history.js` - работа с историей
- ✅ `profile.js` - работа с профилем
- ✅ `quiz_transform.js` - логика квиза
- ✅ `results.js` - обработка результатов
- ✅ `script_chat.js` - UI квиза
- ✅ `script_index.js` - главная страница
- ✅ `notifications.js` - уведомления

## Почему это важно?

### В Docker окружении:
- **Nginx** раздаёт статические файлы из `/usr/share/nginx/html/`
- **Backend** доступен через proxy на `/api`
- Относительные пути работают независимо от структуры URL

### Преимущества:
1. ✅ Работает в любом окружении (dev, staging, production)
2. ✅ Не зависит от настроек сервера
3. ✅ Проще деплой на разных платформах
4. ✅ Меньше проблем с CORS и routing

## Структура после изменений

```
http://localhost/
├── index.html              → Главная страница
├── chat.html               → Квиз
├── history.html            → История
├── profile.html            → Профиль
├── register.html           → Регистрация
├── results.html            → Результаты
├── images/                 → Изображения
│   ├── logo.png
│   └── ...
├── *.css                   → Стили
├── *.js                    → JavaScript
└── /api/*                  → Backend API (проксируется в backend:8000)
```

## API Endpoints

API доступен через относительные пути:
```javascript
// В api.js:
const API_BASE_URL = '';  // Пустая строка = относительные пути

// Примеры:
await fetch('/api/auth/login', { ... });
await fetch('/api/quiz/save', { ... });
await fetch('/api/health', { ... });
```

## Backend изменения

**main.py** - условный mount static для локальной разработки:
```python
# Mount static files - для локальной разработки
# В production (Docker) статика раздаётся через Nginx
frontend_path = Path(__file__).parent.parent / "frontend"
if frontend_path.exists():
    app.mount("/static", StaticFiles(directory=str(frontend_path)), name="static")
```

## Nginx конфигурация

```nginx
server {
    listen 80;
    root /usr/share/nginx/html;
    
    # Frontend - статические файлы
    location / {
        try_files $uri $uri/ /index.html;
    }
    
    # Backend - API proxy
    location /api {
        proxy_pass http://backend:8000;
        # ... остальные настройки proxy
    }
}
```

## Проверка работоспособности

### После деплоя проверьте:

1. **Frontend загружается**
   ```bash
   curl http://localhost/
   ```

2. **Статические файлы доступны**
   ```bash
   curl http://localhost/images/logo.png
   curl http://localhost/base.css
   curl http://localhost/api.js
   ```

3. **API работает**
   ```bash
   curl http://localhost/api/health
   ```

4. **Навигация работает**
   - Открыть http://localhost/
   - Кликнуть "История" → должен открыться /history.html
   - Кликнуть "Профиль" → должен открыться /profile.html
   - Все ссылки должны работать

## Совместимость

✅ **Работает с:**
- Docker + Nginx
- Локальный Python HTTP сервер
- Railway.app
- Vercel
- Netlify
- DigitalOcean App Platform
- AWS S3 + CloudFront
- Любой статический хостинг

✅ **Требования:**
- Backend должен быть доступен на `/api` (через proxy или subdomain)
- Frontend файлы в корне веб-сервера

## Дополнительно

Если нужно развернуть на поддомене (например, app.beautips.com):
- ✅ Ничего менять не нужно!
- Относительные пути будут работать автоматически

Если backend на отдельном домене (api.beautips.com):
- Измените `API_BASE_URL` в `api.js`:
  ```javascript
  const API_BASE_URL = 'https://api.beautips.com';
  ```
- Настройте CORS на backend для домена frontend

---

**Версия**: 2.0.1  
**Дата изменений**: 2026-02-02  
**Статус**: ✅ Готово к деплою
