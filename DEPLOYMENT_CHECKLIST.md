# ✅ Чеклист деплоя BeauTips

## Перед деплоем

### 1. Подготовка окружения
- [ ] Установлен Docker 20.10+
- [ ] Установлен Docker Compose 2.0+
- [ ] Установлен Git
- [ ] Настроен доступ к серверу (SSH)
- [ ] Настроен домен (DNS записи указывают на сервер)

### 2. Получение необходимых ключей

#### Google Gemini API
- [ ] Зарегистрирован аккаунт Google
- [ ] Получен API ключ на https://aistudio.google.com/app/apikey
- [ ] Протестирован API ключ (сделан тестовый запрос)

#### Email (SMTP)
- [ ] Выбран email провайдер (Gmail, Яндекс, Mail.ru)
- [ ] Создан App Password для SMTP
- [ ] Протестирована отправка email

### 3. Настройка базы данных
- [ ] Выбран хостинг для PostgreSQL (встроенный Docker / внешний сервис)
- [ ] Создана база данных
- [ ] Созданы credentials для доступа
- [ ] Записан DATABASE_URL

### 4. Подготовка секретов
- [ ] Сгенерирован SECRET_KEY: `openssl rand -hex 32`
- [ ] Сгенерированы надежные пароли для всех сервисов
- [ ] Секреты записаны в безопасное место (password manager)

---

## Процесс деплоя

### Шаг 1: Клонирование и настройка

```bash
# На сервере
cd /opt
git clone <your-repo-url> beautips
cd beautips

# Создание .env файла
cp .env.example .env
nano .env
```

- [ ] Клонирован репозиторий
- [ ] Создан файл .env
- [ ] Заполнены все обязательные переменные:
  - [ ] `POSTGRES_USER`
  - [ ] `POSTGRES_PASSWORD`
  - [ ] `POSTGRES_DB`
  - [ ] `SECRET_KEY`
  - [ ] `GOOGLE_API_KEY`
  - [ ] `SMTP_USER`
  - [ ] `SMTP_PASSWORD`
  - [ ] `FROM_EMAIL`
  - [ ] `BASE_URL`

### Шаг 2: Запуск приложения

```bash
# Development (HTTP)
./start.sh

# Production (HTTPS с Let's Encrypt)
# 1. Настройте DOMAIN и LETSENCRYPT_EMAIL в .env
# 2. Запустите:
make prod-up
```

- [ ] Приложение запущено
- [ ] Все контейнеры в статусе "healthy": `docker-compose ps`
- [ ] Логи не показывают критических ошибок: `docker-compose logs`

### Шаг 3: Проверка работоспособности

#### Backend
```bash
# Health check
curl http://localhost:8000/api/health

# API документация
curl http://localhost:8000/docs
```

- [ ] Health check возвращает `{"status": "healthy"}`
- [ ] API документация открывается
- [ ] База данных подключена (нет ошибок в логах)

#### Frontend
```bash
# Проверка главной страницы
curl http://localhost/
```

- [ ] Главная страница загружается
- [ ] Статические файлы доступны
- [ ] Нет ошибок 404

#### Database
```bash
# Проверка подключения
docker-compose exec postgres psql -U beautips_user -d beautips_db -c "SELECT 1;"
```

- [ ] PostgreSQL отвечает
- [ ] Таблицы созданы
- [ ] Можно выполнять запросы

### Шаг 4: Тестирование функционала

- [ ] **Регистрация работает**
  - Открыт /register.html
  - Создан тестовый аккаунт
  - Email верификации получен

- [ ] **Вход работает**
  - Успешный вход с созданными credentials
  - JWT токен сохраняется
  - Redirect на главную страницу

- [ ] **Тест работает**
  - Открывается /chat.html
  - Вопросы отображаются
  - Ответы сохраняются
  - AI генерирует рекомендации

- [ ] **Профиль работает**
  - Открывается /profile.html
  - Данные пользователя отображаются
  - История тестов доступна

---

## После деплоя

### 1. Безопасность

- [ ] Настроен Firewall
  ```bash
  sudo ufw allow 80/tcp
  sudo ufw allow 443/tcp
  sudo ufw allow 22/tcp
  sudo ufw enable
  ```

- [ ] HTTPS настроен (для production)
- [ ] SSL сертификат получен от Let's Encrypt
- [ ] HTTP редиректит на HTTPS
- [ ] Секреты не хранятся в Git
- [ ] `.env` добавлен в `.gitignore`

### 2. Мониторинг

- [ ] Настроены логи
  ```bash
  # Просмотр логов
  docker-compose logs -f > /var/log/beautips.log
  ```

- [ ] Настроен log rotation
- [ ] Мониторинг использования ресурсов: `docker stats`
- [ ] Health check работает: `curl http://localhost:8000/api/health`

### 3. Бэкапы

- [ ] Настроен автоматический бэкап базы данных
  ```bash
  # Добавить в crontab
  0 3 * * * /opt/beautips/backup.sh
  ```

- [ ] Протестировано восстановление из бэкапа
- [ ] Бэкапы сохраняются в безопасное место (S3, отдельный сервер)

### 4. Производительность

- [ ] Проверена скорость загрузки страниц
- [ ] Настроено кеширование статики (Nginx)
- [ ] Gzip compression включен
- [ ] Изображения оптимизированы
- [ ] Database indexes созданы (если нужно)

### 5. Документация

- [ ] README.md актуален
- [ ] DEPLOYMENT.md содержит актуальные инструкции
- [ ] .env.example содержит все необходимые переменные
- [ ] API документация доступна на /docs

---

## Production Чеклист

### Критические настройки для production:

- [ ] `DEBUG=False` или отсутствует
- [ ] Используются strong passwords (>32 символов)
- [ ] `SECRET_KEY` уникален для production
- [ ] HTTPS включен
- [ ] CORS настроен для production домена
- [ ] Rate limiting настроен (если нужно)
- [ ] Логи пишутся в файлы, не только в stdout
- [ ] Автоматические обновления системы включены
- [ ] Регулярные бэкапы настроены
- [ ] Monitoring и alerting настроены

### Необязательно, но рекомендуется:

- [ ] CDN для статических файлов
- [ ] Redis для кеширования
- [ ] Horizontal scaling (Load Balancer)
- [ ] Container orchestration (Kubernetes)
- [ ] CI/CD pipeline (GitHub Actions)
- [ ] Staging environment
- [ ] Automated testing
- [ ] Error tracking (Sentry)

---

## Troubleshooting Quick Reference

### Backend не запускается
```bash
docker-compose logs backend
# Проверьте DATABASE_URL и GOOGLE_API_KEY
```

### База данных недоступна
```bash
docker-compose logs postgres
# Проверьте POSTGRES_PASSWORD и доступ к порту 5432
```

### Email не отправляются
```bash
# Для Gmail:
# 1. Включите 2FA
# 2. Создайте App Password
# 3. Используйте App Password в SMTP_PASSWORD
```

### 502 Bad Gateway
```bash
# Проверьте что backend запущен
docker-compose ps
curl http://localhost:8000/api/health
```

---

## Контакты и поддержка

- 📧 Email: support@beautips.com
- 📝 Documentation: /docs
- 🐛 Issues: GitHub Issues
- 💬 Chat: Discord/Slack

---

**Дата**: 2026-02-02  
**Версия чеклиста**: 1.0
