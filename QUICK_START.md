# 🚀 Быстрый старт: Email верификация

## Шаг 1: Выполнить миграцию базы данных

### Вариант A: Через Python скрипт (рекомендуется)

```bash
cd backend
python run_migration.py
```

### Вариант B: Через psql

```bash
psql -U your_username -d beautips_db -f backend/migrations/add_email_verification.sql
```

**Проверка миграции:**
```sql
-- Подключитесь к БД и выполните:
SELECT column_name, data_type 
FROM information_schema.columns
WHERE table_name = 'users' 
  AND column_name IN ('email_verified', 'verification_token', 'verification_token_expires');
```

---

## Шаг 2: Настроить SMTP

### 2.1 Создать .env файл

```bash
cd backend
cp env.example .env
```

### 2.2 Настроить Gmail (самый простой способ)

1. **Включите 2FA** в Google аккаунте:
   - https://myaccount.google.com/security
   - Включите "Двухэтапная аутентификация"

2. **Создайте App Password**:
   - https://myaccount.google.com/apppasswords
   - Выберите "Почта" → "Другое устройство"
   - Название: "BeauTips"
   - Скопируйте 16-символьный пароль (например: `abcd efgh ijkl mnop`)

3. **Отредактируйте `.env`**:
```env
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=abcd efgh ijkl mnop  # Ваш App Password БЕЗ пробелов
FROM_EMAIL=your-email@gmail.com
FROM_NAME=BeauTips
BASE_URL=http://localhost:8000
```

**⚠️ ВАЖНО:** Используйте App Password, а НЕ обычный пароль!

---

## Шаг 3: Перезапустить сервер

```bash
cd backend
python -m uvicorn main:app --reload
```

---

## Шаг 4: Тестирование

### 4.1 Регистрация

1. Откройте: http://localhost:8000/static/register.html
2. Зарегистрируйтесь с реальным email
3. Проверьте почту - должно прийти письмо с кнопкой "Подтвердить Email"

### 4.2 Верификация

1. Кликните на ссылку в письме
2. Откроется страница верификации
3. Должно появиться сообщение "Email Подтверждён! 🎉"
4. Придёт приветственное письмо

### 4.3 Вход

1. Вернитесь на страницу входа
2. Войдите с вашими данными
3. ✅ Должен работать вход!

### 4.4 Тест без верификации

Попробуйте войти сразу после регистрации (до подтверждения email):
- ❌ Должна быть ошибка: "Please verify your email before logging in"

---

## 🐛 Troubleshooting

### Проблема: "Письма не отправляются"

**Решение:**
1. Проверьте логи сервера на ошибки
2. Убедитесь, что используете App Password (не обычный пароль)
3. Проверьте настройки в `.env`
4. Для Gmail убедитесь, что 2FA включена

### Проблема: "Invalid verification token"

**Решение:**
- Токен истёк (24 часа) - запросите новое письмо
- Или используйте `/api/auth/resend-verification`

### Проблема: "Please verify your email" при входе

**Решение:**
- Проверьте почту и подтвердите email
- Или запросите новое письмо через `/api/auth/resend-verification`

---

## ✅ Чеклист

- [ ] Миграция выполнена (`python run_migration.py`)
- [ ] `.env` файл создан и заполнен
- [ ] SMTP настройки протестированы (письмо приходит)
- [ ] Сервер перезапущен
- [ ] Регистрация работает
- [ ] Письмо получено
- [ ] Верификация работает
- [ ] Вход после верификации работает

---

## 📚 Полная документация

См. `EMAIL_VERIFICATION_GUIDE.md` для детальной информации.

---

**Готово! 🎉** Теперь у вас работает полная система верификации email!

