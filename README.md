# BeauTips - Full Stack Quiz Application

Полноценное приложение для косметического теста с системой аутентификации и интеграцией с AI.

## 📁 Структура проекта

```
ФМП сайт/
├── backend/          # FastAPI backend
│   ├── main.py       # Главный сервер
│   ├── database.py   # Подключение к БД
│   ├── models.py     # Модели SQLAlchemy
│   ├── schemas.py    # Pydantic схемы
│   ├── auth.py       # JWT аутентификация
│   ├── ai_service.py # Gemini AI + RAG система
│   ├── requirements.txt
│   ├── .env          # Переменные окружения
│   └── routers/      # API роутеры
│
├── frontend/         # HTML/CSS/JS frontend
│   ├── register.html # Регистрация/вход
│   ├── chat.html     # Интерфейс квиза
│   ├── index.html    # Главная страница
│   ├── api.js        # API клиент
│   ├── auth.js       # Авторизация
│   ├── quiz_transform.js  # Логика квиза
│   ├── script_chat.js     # UI квиза
│   ├── *.css         # Стили
│   └── images/       # Изображения
│
├── index.html        # Лендинг
├── index.css         # Стили лендинга
└── README.md         # Этот файл
```

## 🚀 Быстрый старт

### 1. Настройка Backend

```bash
# Создать conda окружение с Python 3.11
conda create -n fastapi_todo python=3.11
conda activate fastapi_todo

# Установить зависимости
cd backend
pip install -r requirements.txt

# Настроить .env файл с DATABASE_URL и GOOGLE_API_KEY

# Запустить сервер
python -m uvicorn main:app --reload --host 127.0.0.1 --port 8000
```

Backend будет доступен на: **http://127.0.0.1:8000**  
API документация: **http://127.0.0.1:8000/docs**

### 2. Запуск Frontend

Откройте `frontend/register.html` в браузере или используйте локальный сервер:

```bash
# Простой HTTP сервер Python
cd ..
python -m http.server 8080

# Затем откройте: http://localhost:8080/frontend/register.html
```

## 🔐 База данных

**PostgreSQL**: `beautips_mvp`
- Таблицы: `users`, `quiz_sessions`, `quiz_answers`, `ai_recommendations`, `products`, `reviews`
- Данные сохраняются в формате вопрос-ответ на русском языке

## ✨ Возможности

- ✅ Регистрация и вход с JWT токенами
- ✅ Косметический тест с множественным выбором
- ✅ Автоматическое сохранение ответов на сервер
- ✅ AI-рекомендации с использованием Gemini 2.5 Flash
- ✅ RAG система с реальными товарами и отзывами
- ✅ Премиум дизайн интерфейса
- ✅ Проверка авторизации

## 📝 API Endpoints

### Аутентификация
- `POST /api/auth/register` - Регистрация
- `POST /api/auth/login` - Вход

### Тест
- `POST /api/quiz/save` - Сохранить ответы
- `GET /api/quiz/{session_id}` - Получить ответы
- `POST /api/quiz/submit` - Отправить на обработку AI

## 🧪 Тестирование

### Тест через браузер:
1. Зарегистрируйтесь на `/frontend/register.html`
2. Войдите в систему
3. Пройдите тест на `/frontend/chat.html`
4. Проверьте данные в БД:

```sql
SELECT * FROM quiz_answers;
SELECT * FROM ai_recommendations;
```

## 🤖 AI Integration

Проект использует:
- **Gemini 2.5 Flash** для генерации рекомендаций
- **RAG система** с embedded базой товаров и отзывов
- **SentenceTransformer** для семантического поиска

---

**Версия**: 2.0.0  
**Дата**: 2026-01-30

